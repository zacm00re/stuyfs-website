const app = require('express')()
const fs = require('fs')

app.post('/formsubmit', (request, response) => { 
    /*
    take form
    write to text file
    respond with link back to html
    */
})

app.listen(777)